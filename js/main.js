// Criar variáveis no Javascript
// var letra = '0'; var é uma versão e, desuso
// const letra = 'D';
// console.log(letra);

// Iniciando uma variável que representa um jogador
let letra = 'X';

function joga() {
    let blocoClicado = document.getElementById(cel).innerHTML;
    // innerHTML - injeta tags HTML na página 
    if(blocoClicado == "X");
}